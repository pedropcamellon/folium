# Optional backend overrides. Root .env loads first.
# Copy to backend/.env only when host-run backend values differ from the baseline.

STORAGE_ENDPOINT=http://localhost:9000
STORAGE_PUBLIC_ENDPOINT=http://localhost:9000

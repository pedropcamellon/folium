include:
  - docker-compose.database.yml
  - docker-compose.storage.yml
  - backend/docker-compose.yml
  - frontend/docker-compose.yml
  - services/temporal/docker-compose.infra.yml
  - services/transcribe/docker-compose.yml
  - services/summarize/docker-compose.yml
  - services/voice_note_workflow/docker-compose.service.yml
  - services/observability/docker-compose.observability.yml

services:
  backend:
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy

/**
 * Auth layout - minimal layout without navigation for login/register pages
 */

export default function AuthLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}

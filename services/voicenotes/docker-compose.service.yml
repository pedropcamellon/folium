services:
  folium-voicenotes-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: folium-voicenotes-worker
    env_file:
      - path: ../../.env
        required: false
      - path: .env
        required: false
    depends_on:
      southdrift-backend:
        condition: service_healthy
      folium-summarize:
        condition: service_healthy
      southdrift-temporal:
        condition: service_healthy
      southdrift-transcribe:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - TEMPORAL_ADDRESS=southdrift-temporal:7233
      - TEMPORAL_NAMESPACE=default
      - VOICE_NOTE_TASK_QUEUE=voice-notes-queue
      - BACKEND_API_URL=http://southdrift-backend:8000
      - TRANSCRIPTION_SERVICE_URL=http://southdrift-transcribe:8001
      - SUMMARIZATION_SERVICE_URL=http://folium-summarize:8002
      - LOG_LEVEL=INFO
    restart: unless-stopped
    develop:
      watch:
        - action: sync
          path: ./app
          target: /app/app
          ignore:
            - __pycache__/
            - "*.pyc"
            - .pytest_cache/
        - action: rebuild
          path: pyproject.toml

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://folium-prometheus:9090
    uid: prometheus
    isDefault: true
    editable: true

  - name: Loki
    type: loki
    access: proxy
    url: http://folium-loki:3100
    uid: loki
    isDefault: false
    editable: true
    jsonData:
      maxLines: 1000

# Populate all source fields from a verified publisher release before enabling downloads.
# The runner refuses incomplete artifacts and verifies size and SHA-256 after download.
[artifact]
name = "mediphi-clinical-q4-k-m"
url = ""
sha256 = ""
size_bytes = 0
destination = "services/summarize/models/model.gguf"